# 🚀 P3 Unlock — Быстрый Старт

## Что делать дальше (порядок действий)

### ✅ Система уже готова

Все компоненты реализованы и работают:
- ✅ 12 Entities засижены (Exchanges, Protocols, Issuers)
- ✅ 25 SignalContexts созданы (5 режимов)
- ✅ 25 Live decisions сгенерированы
- ✅ Bootstrap endpoints работают
- ✅ ERC-20 Indexer активен
- ✅ Background jobs запущены

---

## 🎯 Автоматическая разблокировка

### Вариант 1: Запустить automation script (рекомендуется)

```bash
# Запустить в background
cd /app/scripts
nohup ./p3_bootstrap.sh > /tmp/p3_bootstrap.log 2>&1 &

# Мониторить прогресс
tail -f /tmp/p3_bootstrap.log
```

**Что делает скрипт:**
- Генерирует decisions каждые 15 минут
- Проверяет coverage variance
- Останавливается при P3 unlock
- Всего 12 запусков (3 часа)

---

### Вариант 2: Ручной запуск

```bash
# 1. Генерировать decisions (запускать каждые 10-15 мин)
curl -X POST http://localhost:8002/api/engine/bootstrap/decisions \
  -H "Content-Type: application/json" \
  -d '{"decisionsPerRegime":20,"windows":["1h","6h","24h"]}'

# 2. Проверить статус P3
curl -s http://localhost:8002/api/engine/bootstrap/status | jq '.data'

# 3. Повторять пункт 1-2 до разблокировки
```

---

## 📊 Мониторинг прогресса

### Проверить текущий статус

```bash
curl -s http://localhost:8002/api/engine/bootstrap/status | jq '{
  decisions: .data.decisions.total,
  contexts: .data.contexts.total,
  coverage: .data.coverage,
  p3: .data.p3
}'
```

**Ожидаемый output (после 2-3 часов):**

```json
{
  "decisions": 300,
  "contexts": 25,
  "coverage": {
    "min": 45,
    "max": 68,
    "variance": 23
  },
  "p3": {
    "unlocked": true,
    "status": "✅ P3 ACTIVE"
  }
}
```

---

## 🎯 Критерии разблокировки

P3 откроется автоматически, когда:

| Критерий | Цель | Текущее |
|----------|------|---------|
| Live snapshots | ≥ 300 | 25 |
| Coverage variance | > 0 | 0 |
| Decision diversity | ≥ 2 buckets | 1 |
| Delayed labels | ≥ 30% | - |
| ML targets v1.1 | ≥ 20% | - |

---

## ⚠️ Важно понимать

### Почему variance = 0 сейчас?

Coverage рассчитывается на основе **реальных indexed данных**:
- Количество contexts в БД (из indexer)
- Количество actors в БД (из transactions)
- Количество signals в БД (из bundles)

Bootstrap contexts — это **синтетические данные** для демонстрации.  
Они НЕ влияют на runtime coverage calculation.

### Когда появится variance?

По мере работы системы:
1. ERC-20 Indexer индексирует блоки (каждые 15 сек)
2. Build Signals job создает реальные signals
3. ActorSignals job обогащает contexts
4. Coverage начнет меняться естественно

**Время:** 2-4 часа непрерывной работы

---

## 🔍 Проверка здоровья системы

### 1. Backend работает

```bash
curl http://localhost:8001/api/health
# {"ok":true,"ts":...,"uptime":...}
```

### 2. Indexer активен

```bash
tail -f /var/log/supervisor/backend.out.log | grep Indexer
# [ERC20 Indexer] Synced blocks...
```

### 3. Background jobs работают

```bash
curl -s http://localhost:8002/api/health | jq '.jobs'
```

---

## 🚦 Что делать после P3 unlock

Когда P3 разблокируется:

### 1. ML Training v1.1

```bash
# ML модель начнет обучаться на Live данных
# Targets v1.1 станут доступны
# Coverage variance будет использоваться для feature engineering
```

### 2. Переход на P4 (Shadow Eval v1.1)

```bash
# Обученная модель начнет работать в shadow mode
# Predictions будут логироваться но не использоваться
# Качество будет оцениваться против Live (Gold) decisions
```

### 3. Мониторинг качества

```bash
# Проверить accuracy shadow predictions
# Сравнить с baseline (Engine v1.1)
# Принять решение о promotion в Production
```

---

## 📋 Чек-лист

- [x] Проект развернут
- [x] API ключи интегрированы (Infura, Ankr, Telegram)
- [x] 12 Entities засижены
- [x] 25 Bootstrap contexts созданы
- [x] 25 Decisions сгенерированы
- [x] Bootstrap endpoints работают
- [ ] Запустить p3_bootstrap.sh
- [ ] Дождаться 300+ decisions
- [ ] Дождаться variance > 0
- [ ] P3 разблокирован ✅
- [ ] ML Training v1.1 активен
- [ ] P4 Shadow Mode запущен

---

## 🆘 Troubleshooting

### Если variance не появляется через 4 часа

**Проверить:**

1. ERC-20 Indexer работает:
```bash
tail -f /var/log/supervisor/backend.out.log | grep "ERC20 Indexer"
```

2. Signals генерируются:
```bash
curl -s http://localhost:8002/api/signals/stats | jq '.data'
```

3. MongoDB доступна:
```bash
mongosh blockview --eval "db.stats()" --quiet
```

### Если нужна помощь

См. детальную документацию:
- `/app/P3_UNLOCK_STATUS.md` - полный архитектурный отчет
- `/app/DEPLOYMENT_SUMMARY.md` - общая документация по системе
- `/app/ENGINE_V1_1_RELEASE_NOTES.md` - Engine v1.1 спецификация

---

## ✅ Итого

**Все готово для P3 unlock!**

Просто запустите:
```bash
cd /app/scripts && ./p3_bootstrap.sh
```

И дайте системе 2-4 часа. P3 разблокируется автоматически. 🚀
